@model ErrorViewModel

@{
    ViewData["Title"] = "Ошибка";
}

<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow-lg p-4 rounded-3" style="max-width: 500px; width: 100%; background-color: #f8f9fa;">
        <div class="card-body text-center">
            <h1 class="display-3 text-danger">Упс!</h1>
            <h2 class="text-dark">Что-то пошло не так...</h2>
            <p class="text-muted">Мы приносим извинения за неудобства. Попробуйте ещё раз позже или свяжитесь с
                поддержкой, если проблема повторяется.</p>

            @if (Model.StatusCode.HasValue)
            {
                <p class="mt-3"><strong>Код ошибки:</strong> @Model.StatusCode</p>
            }

            @if (Model.ShowRequestId)
            {
                <p class="mt-3"><strong>ID запроса:</strong> <code>@Model.RequestId</code></p>
            }

            <h3 class="mt-4">Что вы можете сделать?</h3>
            <ul class="list-unstyled text-muted">
                <li><a href="/" class="text-primary">Вернуться на главную страницу</a>.</li>
                <li>Попробовать обновить страницу.</li>
                <li>Связаться с <a href="mailto:support@yourdomain.com" class="text-primary">поддержкой</a>, если
                    проблема сохраняется.
                </li>
            </ul>

            <div class="mt-4">
                <h4 class="text-dark">Работаете в режиме разработки?</h4>
                <p class="text-muted">
                    Если вы работаете в <strong>режиме разработки</strong>, вы получите более подробную информацию об
                    ошибке.
                </p>
                <p class="text-muted">
                    Однако, <strong>режим разработки не рекомендуется использовать в продакшене</strong>, так как это
                    может раскрыть конфиденциальную информацию.
                </p>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .card:hover {
        transform: translateY(-10px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }

    .text-danger {
        font-weight: bold;
        color: #dc3545;
    }

    .text-dark {
        font-size: 1.25rem;
        font-weight: 600;
    }

    .text-muted {
        font-size: 1rem;
        line-height: 1.5;
    }

    .text-primary {
        text-decoration: none;
        font-weight: 500;
        color: #007bff;
    }

    .text-primary:hover {
        text-decoration: underline;
        color: #0056b3;
    }
</style>
